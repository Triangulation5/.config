# Things I believe that I can add to my shell
Sorted via difficulty.

## Easy
- Minimalize the amount of code that is used in this shell. Make sure that
reused code blocks can become reusable components instead of just rewriting
similar things.
- Make every surface more lazy loaded (already pretty well implemented). Make
it less heavy on the Ram, Cpu, and iGpu machines by killing background
surfaces after they have not been loaded for a certain amount of time.
- Make everything asynchronous.
- Clean up the codebase and clean up comments (make sure they are all docstyle).

## Medium
- Split up monolithic files and make this more modular. Prioritize easy to
  clean ones.

- Add some kind of profile data surface, with your user icon and user name and
  stuff like (josh@core). With stats like a `fastfetch` type thing info page.

- I like how the pill is used to install applications, via dragging installers
  on it, we need more dynamic features like that.

## Hard
- Iphone style dynamic island face id verification on the lockscreen and stuff.

- Make authentications prompts from fedora run on the pill.

- Make the lockscreen use fingerprint and make that a toggle and make is so
  that the fingerprint daemon gets restarted if it does not work so that
  fingerprint can be always enabled.

- Add custom theme switcher (vague theme, old ricelin theme)

- Add AI like Google Gemini Assistant on android phone style assistant.

- Open terminal applications via the launcher.

# Asking AI what I can add

## Easy

## Medium

## Hard
